# Splash screen demo video

Re-render (from the project root, with Playwright and ffmpeg installed; timings are constants at the top of the script): `node marketing/splash-demo/make-demo.mjs --rebuild`
